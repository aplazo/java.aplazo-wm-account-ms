management:
  endpoint:
    health:
      enabled: true
      show-details: always
    info:
      enabled: true
  endpoints:
    enabled-by-default: false
    web:
      exposure:
        include: health,info
spring:
  profiles:
    active: api
aplazo:
 openapi:
  generate: ${APLAZO_GENERATE_OPENAPI:false}
 url:
  api:
   # auth-hydra token revocation (C-NEW-01). S2S via IAM + CloudMap; default for dev/local/test.
   auth-hydra-url: ${AUTH_HYDRA_URL:http://localhost:4445}
   auth-hydra:
    # Synchronous blocking timeouts (BNPL-977 NFR — value to be validated with the squad).
    connect-timeout-ms: ${AUTH_HYDRA_CONNECT_TIMEOUT_MS:2000}
    read-timeout-ms: ${AUTH_HYDRA_READ_TIMEOUT_MS:5000}
   # partners-ms base URL for the post-Unlink notification (C-NEW-02). Default for dev/local/test.
   partners-url: ${PARTNERS_MS_URL:http://localhost}
api:
  aplazo:
    security:
      secret: ${APLAZO_SDK_SECRET:secreto}
    oauth:
      # OAuth2 client the Hydra revocation is scoped to (single Walmart/Cashi client). Default for dev/local/test.
      client-id: ${WM_OAUTH_CLIENT_ID:wm-cashi-client}